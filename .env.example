# uSwap Zero — Environment Variables
# Copy to .env for local development (never commit .env)

# Required in production — 64-char hex key for AES-256-GCM order token encryption
# Generate: openssl rand -hex 32
ORDER_SECRET=

# Optional — JWT from NEAR Intents partners portal (enables 0% protocol fee)
# key_type: distribution_channel — used for the 1Click swap API
NEAR_INTENTS_JWT=

# Optional — JWT for the NEAR Intents Explorer API (reseller monitor)
# key_type: explorer — DIFFERENT key from NEAR_INTENTS_JWT, get from partners.near-intents.org
# Rate limit: 1 request / 5 seconds per partner ID
NEAR_INTENTS_EXPLORER_JWT=

# Optional — Override NEAR Intents API base URL
# Default: https://1click.chaindefuser.com
NEAR_INTENTS_API_URL=

# Optional — HTTP listen port (default: 3000)
PORT=3000

# --- Telegram Bot (optional) ---
# If unset, bot is disabled and the app runs as web-only.

# Bot token from @BotFather
TG_BOT_TOKEN=

# Public base URL of this deployment (used for webhook registration + order deep links)
# Example: https://zero.uswap.net
TG_APP_URL=

# Webhook secret — auto-generated on startup if not set
TG_WEBHOOK_SECRET=

# --- Reseller Monitor (optional) ---
# Polls NEAR Intents Explorer API for Swap.my / LizardSwap / EagleSwap transactions.
# Posts fee cards to Telegram group threads and live-updates thread titles + channel description.
# Requires TG_BOT_TOKEN to be set. Disabled if TG_MONITOR_GROUP_ID is unset.

# Telegram supergroup ID with forum topics enabled
TG_MONITOR_GROUP_ID=

# Forum topic (thread) IDs for each reseller — get from t.me/c/<group_id>/<thread_id>
TG_SWAPMY_THREAD_ID=
TG_EAGLESWAP_THREAD_ID=
TG_LIZARDSWAP_THREAD_ID=

# Channel or group where the bot updates the description live.
# Put $ anywhere in the description — bot replaces it with the running total.
# Example description: "Don't be a part of the $, use uSwap Zero"
TG_MAIN_CHAT_ID=
